import { MODE } from '../../utils/types';
export declare type BUTTON_TYPES = 'submit' | 'button' | 'reset';
export declare class Checkbox {
  ID: string;
  el: HTMLElement;
  class: string;
  mode: MODE;
  disabled: boolean;
  block: boolean;
  switchable: boolean;
  checked: boolean;
  value: string;
  handleOnChange(e: any): void;
  componentWillLoad(): void;
  render(): any;
}
