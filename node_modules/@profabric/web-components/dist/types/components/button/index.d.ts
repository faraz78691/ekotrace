import { MODE, SIZE, THEME } from '../../utils/types';
export declare type BUTTON_TYPE = 'submit' | 'button' | 'reset';
export declare class Button {
  tempDisabled: boolean;
  el: HTMLElement;
  theme: THEME | string;
  size: SIZE;
  mode: MODE;
  type: BUTTON_TYPE;
  loading: boolean;
  disabled: boolean;
  block: boolean;
  watchLoading(currentValue: boolean): void;
  onClick(event: Event): void;
  componentWillLoad(): void;
  render(): any;
}
